import { CommentModel } from "../../models/commentsModel";

export async function addComment(req: any, res: any) {
    // try {
    //     const {  } = req.body;

    //     if() {
    //         throw new Error('Please fill all fields');
    //     }

    //     //send request to DB
    //     await CommentModel.create({
            
    //     });

    //     return res.status(201).send({ message: "comment added successfully" });

    // } catch (error: any) {
    //     console.error(error);
    //     return res.status(500).send({ error: error.message });
    // }
};


export async function getCommentsByProduct(req: any, res: any) {
    // try {
    //     const {  } = req.body;

    //     if() {
    //         throw new Error('Please fill all fields');
    //     }

    //     //send request to DB
    //     await CommentModel.create({
            
    //     });

    //     return res.status(201).send({ message: "comment added successfully" });

    // } catch (error: any) {
    //     console.error(error);
    //     return res.status(500).send({ error: error.message });
    // }
};

export async function deleteComment(req: any, res: any) {
    // try {
    //     const {  } = req.body;

    //     if() {
    //         throw new Error('Please fill all fields');
    //     }

    //     //send request to DB
    //     await CommentModel.create({
            
    //     });

    //     return res.status(201).send({ message: "comment added successfully" });

    // } catch (error: any) {
    //     console.error(error);
    //     return res.status(500).send({ error: error.message });
    // }
};